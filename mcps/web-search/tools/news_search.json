{
    "name": "news_search",
    "description": "Search news using 6 parallel engines (Bing/Baidu/360/Sogou/Shenma/Toutiao) with DDG fallback. Returns recent news articles with title, source, date, URL and excerpt. Use for current events and news queries.\n\nFIELD NAME: The only required parameter is 'query' (NOT 'topic', NOT 'keywords').",
    "inputSchema": {
        "type": "object",
        "properties": {
            "query": {
                "type": "string",
                "description": "Search query string (required). Do NOT use 'topic' or 'keywords' as field name."
            },
            "max_results": {
                "type": "integer",
                "description": "Maximum number of results (default: 5, max: 20)",
                "default": 5
            },
            "region": {
                "type": "string",
                "description": "Region code (wt-wt: worldwide, cn-zh: China)",
                "default": "wt-wt"
            },
            "timelimit": {
                "type": "string",
                "description": "Time limit (d: day, w: week, m: month)",
                "enum": ["d", "w", "m"]
            }
        },
        "required": ["query"]
    }
}
